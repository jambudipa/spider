# Test infrastructure

This folder creates Effect runtimes, controlled time, and rate-limit support
for tests. It isolates infrastructure concerns from test intent.

Use these utilities when tests need a predictable service layer. Keep real
network behavior outside this folder.
