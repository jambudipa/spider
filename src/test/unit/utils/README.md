# Utility unit tests

This folder tests URL, JSON, regular-expression, and deduplication utilities.
These tests protect deterministic pure behavior.

Keep fixture input minimal. Add integration coverage only when a utility crosses
a real I/O boundary.
