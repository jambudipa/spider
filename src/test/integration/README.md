# Integration tests

This folder tests services across real boundaries such as HTTP origins,
session validation, and interaction discovery. Start with the named service
test that matches the feature under change.

Keep each test focused on one integration contract. Use scenario tests for
website-specific behavior.
