# Configuration unit tests

This folder tests validation and resolution of crawler configuration values.
It covers defaults, equivalence rules, and user-agent strategy behavior.

Keep tests synchronous where possible. Test service layers elsewhere when a
case requires an Effect runtime.
