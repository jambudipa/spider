# Anti-block scenarios

This folder tests crawler behavior on block pages, referer checks, and
persistent cookie blocks. These cases use live site responses.

Treat external page changes as test evidence. Do not weaken assertions without
confirming the upstream behavior changed.
