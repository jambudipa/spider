# Core crawler unit tests

This folder tests crawl lifecycle, retries, queue behavior, interruption, and
result handling with controlled dependencies.

Preserve deterministic service layers. Use integration tests for actual network
or browser effects.
