# Dynamic-content scenarios

This folder tests behavior that appears after scrolling, button actions, or
background GraphQL requests. These tests require browser timing awareness.

Use explicit readiness conditions. Avoid fixed delays when a page signal can
prove the same result.
