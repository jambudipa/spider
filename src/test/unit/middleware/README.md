# Middleware unit tests

This folder tests request and response middleware ordering and transformations.
It protects the pipeline contract without starting a crawl.

Add a test here when middleware behavior changes. Do not duplicate general
crawler lifecycle tests.
