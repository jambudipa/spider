# Example integration tests

This folder verifies that the shipped examples keep their expected Effect v4
composition and output behavior.

Update these tests when an example changes its documented contract. Do not
turn them into broad crawler behavior tests.
