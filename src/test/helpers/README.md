# Test helpers

This folder provides reusable fixtures and extraction helpers for crawler
tests. Start with `TestHelper.ts` for common setup.

Keep helpers deterministic and focused on test arrangement. Do not put product
behavior or assertions here.
